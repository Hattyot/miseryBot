const raffle = require("../modules/data.js").raffle
module.exports = async (bot, member) => {
    // raffle.findOneAndRemove({user_ID: member.user.id}, (err, data) => {
    //     console.log(data)
    // })
};
